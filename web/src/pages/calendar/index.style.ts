import styled from 'styled-components'

export const CellList = styled.ul`
  list-style-type: none;
  margin: 0;
  padding: 0;
`

export const CellListStatus = styled.div`
  position: absolute;
  left: 6px;
  top: 4px;
  border-bottom: 1px solid;
`
